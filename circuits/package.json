{
  "name": "@signet/circuits",
  "version": "1.0.0",
  "description": "Self-compiled ZK Email circuit for Signet sybil resistance",
  "type": "module",
  "scripts": {
    "compile":   "node scripts/compile.mjs",
    "setup":     "node scripts/setup.mjs",
    "prove":     "node scripts/prove.mjs",
    "verify":    "node scripts/verify.mjs",
    "build":     "npm run compile && npm run setup"
  },
  "dependencies": {
    "@zk-email/circuits": "^6.3.4",
    "@zk-email/helpers":  "^6.4.2",
    "snarkjs":            "^0.7.5"
  }
}
